// lib/screens/admin/bus_approval_screen.dart

import 'package:flutter/material.dart';
import '../../widgets/common/app_layout.dart';

class BusApprovalScreen extends StatelessWidget {
  const BusApprovalScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const AppLayout(
      title: 'Bus Approval',
      child: Center(
        child: Text('Bus Approval - To be implemented'),
      ),
    );
  }
}