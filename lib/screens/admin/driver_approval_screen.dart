// lib/screens/admin/driver_approval_screen.dart

import 'package:flutter/material.dart';
import '../../widgets/common/app_layout.dart';

class DriverApprovalScreen extends StatelessWidget {
  const DriverApprovalScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const AppLayout(
      title: 'Driver Approval',
      child: Center(
        child: Text('Driver Approval - To be implemented'),
      ),
    );
  }
}